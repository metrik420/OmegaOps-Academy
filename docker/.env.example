# =============================================================================
# OmegaOps Academy - Environment Variables Template
# =============================================================================
#
# This file documents all environment variables used by the application.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env file to version control
# - Add .env to your .gitignore
# - Keep this .env.example file as documentation
# - Use strong, unique values for all secrets
# - Rotate secrets periodically (every 90 days minimum)
#
# Usage:
#   cp docker/.env.example docker/.env
#   # Edit .env with your actual values
#   docker compose -f docker/docker-compose.yml up -d
#
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# -----------------------------------------------------------------------------
# Determines which configuration set to use.
# Options: development, staging, production
#
# Effects:
# - Logging verbosity (production = less verbose)
# - Error detail exposure (production = generic errors)
# - Performance optimizations (production = aggressive caching)
# - Security strictness (production = strictest)
NODE_ENV=production

# -----------------------------------------------------------------------------
# TIMEZONE
# -----------------------------------------------------------------------------
# Container timezone for logs and scheduled tasks.
# Format: TZ database name (e.g., America/New_York, Europe/London, Asia/Tokyo)
# List: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
#
# UTC is recommended for servers:
# - No daylight saving time changes
# - Easy to convert to any local time
# - Industry standard for logs and databases
TZ=UTC

# -----------------------------------------------------------------------------
# APPLICATION URLs (FOR FUTURE USE)
# -----------------------------------------------------------------------------
# Public URL where the application is accessible.
# Used for generating absolute URLs (emails, social sharing, etc.)
#
# APP_URL=https://learn.metrikcorp.com

# Frontend and backend URLs (if different)
# FRONTEND_URL=https://learn.metrikcorp.com
# BACKEND_URL=https://api.learn.metrikcorp.com

# -----------------------------------------------------------------------------
# BACKEND API CONFIGURATION (FOR PHASE 2)
# -----------------------------------------------------------------------------
# When backend runs in a container, these configure it.

# Port the backend listens on inside the container
# BACKEND_PORT=3001

# Database path for SQLite (better-sqlite3)
# DATABASE_PATH=/app/data/omegaops.db

# JWT secret for authentication tokens
# CRITICAL: Use a strong random string (minimum 32 characters)
# Generate: openssl rand -base64 32
# JWT_SECRET=your-super-secret-jwt-key-change-this-immediately

# JWT token expiration time
# Format: Xd (days), Xh (hours), Xm (minutes)
# JWT_EXPIRY=7d

# Session secret for Express sessions
# SESSION_SECRET=another-strong-random-string-at-least-32-chars

# -----------------------------------------------------------------------------
# EXTERNAL SERVICES (FOR FUTURE INTEGRATIONS)
# -----------------------------------------------------------------------------

# Email service (SendGrid, Mailgun, etc.)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM=noreply@metrikcorp.com

# Error tracking (Sentry)
# SENTRY_DSN=https://key@sentry.io/project

# Analytics (if self-hosted)
# ANALYTICS_KEY=your-analytics-key

# AI/LLM API keys (for AI-powered features)
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=your-anthropic-key

# -----------------------------------------------------------------------------
# NGINX PROXY MANAGER CONFIGURATION
# -----------------------------------------------------------------------------
# These are informational - NPM is configured via its web UI.

# Domain name for the application
# DOMAIN=learn.metrikcorp.com

# SSL certificate email (for Let's Encrypt)
# SSL_EMAIL=admin@metrikcorp.com

# -----------------------------------------------------------------------------
# MONITORING & OBSERVABILITY (FOR FUTURE USE)
# -----------------------------------------------------------------------------

# Grafana admin password
# GRAFANA_ADMIN_PASSWORD=strong-grafana-password

# Prometheus scrape interval
# PROMETHEUS_SCRAPE_INTERVAL=15s

# Alert notification endpoints
# ALERT_WEBHOOK_URL=https://hooks.slack.com/services/xxx
# ALERT_TELEGRAM_TOKEN=bot-token
# ALERT_TELEGRAM_CHAT_ID=chat-id

# -----------------------------------------------------------------------------
# BACKUP CONFIGURATION (FOR FUTURE USE)
# -----------------------------------------------------------------------------

# Backup retention (number of backups to keep)
# BACKUP_RETENTION=30

# Remote backup storage (S3, rclone)
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_BUCKET=omegaops-backups
# AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# RATE LIMITING & SECURITY (FOR FUTURE USE)
# -----------------------------------------------------------------------------

# API rate limit (requests per window)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=https://learn.metrikcorp.com,https://www.learn.metrikcorp.com

# Trusted proxy IPs (for getting real client IP behind reverse proxy)
# TRUST_PROXY=loopback,linklocal,uniquelocal

# -----------------------------------------------------------------------------
# DEVELOPMENT OVERRIDES (DO NOT USE IN PRODUCTION)
# -----------------------------------------------------------------------------

# Enable debug mode (verbose logging, stack traces)
# DEBUG=true

# Disable security features for local testing
# DISABLE_RATE_LIMIT=false
# DISABLE_CSRF=false
# DISABLE_HELMET=false

# Use self-signed certificates (NEVER in production)
# ALLOW_INSECURE_TLS=false

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Variable Naming Convention:
#    - UPPERCASE_WITH_UNDERSCORES
#    - Prefix with service name if ambiguous (DB_, SMTP_, etc.)
#
# 2. Secret Generation Commands:
#    - Random string: openssl rand -base64 32
#    - UUID: uuidgen
#    - Password: pwgen -s 32 1
#
# 3. Validation:
#    - After setting values, test that application starts correctly
#    - Check logs for "missing environment variable" errors
#    - Verify secure connections work (database, email, etc.)
#
# 4. Docker Compose Usage:
#    - Place .env file in same directory as docker-compose.yml
#    - Or specify with --env-file flag
#    - Variables in .env auto-substitute ${VAR} in compose file
#
# 5. Security Best Practices:
#    - Minimum 32 characters for secrets
#    - No dictionary words or patterns
#    - Different secrets for dev/staging/production
#    - Regular rotation schedule
#    - Monitor for leaked secrets (GitHub scanning, etc.)
#
# =============================================================================
